# 功率PCB布局 监督/模仿学习建模报告

## 1. 运行环境（按你的要求，训练不在本地跑）

- 服务器：`region-46.seetacloud.com:26032`（AutoDL 容器，Tesla V100-PCIE-32GB）
- Python：`/root/miniconda3/bin/python` 3.8.10，torch 2.0.1+cu118，CUDA 可用
- 工作目录：`/root/pcb-ml`
- 上机前已清除旧的 DreamPlace / Cypress 内容（`/root/DREAMPlace`、`/root/Cypress`、
  conda 环境 `cypress`、`plot_cypress.py`、`plot_placement.py`、`cloud_setup_gpu.sh`），
  释放约 8 GB（可用空间 11 GB → 19 GB）。`/root/autodl-pub` 公共只读挂载与
  `/root/autodl-tmp` 的 AutoDL 系统文件未改动。

## 2. 数据

| 表 | 行数 |
|---|---:|
| boards | 1,489 |
| projects | 1,010 |
| components | 99,109 |
| pad_nets | 379,691 |

关联键 `project_uuid`（分组切分）、`board_id`（同板约束）。

## 3. 任务定义

同一块板上共享 ≥1 个网络的元件对：
- 正样本：同一层且中心距 ≤ 5 mm
- 负样本：距离 ≥ 15 mm
- 5–15 mm 丢弃（模糊）
- 特征只用网表与元件属性，不含坐标/层/旋转，无空间泄漏
- 5 折按项目分组，同一项目的多块板不跨训练/测试

## 4. 结果（全部在服务器上同源跑出）

| 模型 | ROC-AUC | PR-AUC | 备注 |
|---|---:|---:|---|
| 共享信号网络规则 | 0.6961 | 0.2645 | 无需训练 |
| 逻辑回归（57 维手工特征） | **0.8551** | **0.6120** | 717,600 对 / 981 项目 |
| 混合 GNN（二部图 + 成对特征） | **0.8709** | 0.5968 | 743,957 对 / 1,352 板 |

GNN 各折 AUC：0.8815 / 0.8781 / 0.8804 / 0.8854 / 0.8729（方差很小，训练稳定）。

### 怎么读这两个数

- **AUC**：GNN 更高（0.8709 vs 0.8551，+0.016），说明图结构确实带来了额外的排序能力。
- **AP（PR-AUC）**：逻辑回归略高（0.6120 vs 0.5968），说明在"高精度挑选"这个用途上，
  手工特征版本目前更稳。
- 两个模型互补，不是谁全面压倒谁。两者都远高于规则基线（0.6961）。

### 逻辑回归学到的主要规律

- `has_shared_signal` +1.17：共享非电源网络 → 强正相关
- `min_shared_signal_degree` −0.77：共享网络度数越小越可能靠近
- `n_lowdeg_le4` +0.66：共享低度数网络 → 靠近
- `deg_sum / deg_a / deg_b` 负值：引脚越多的器件越倾向铺开
- `prefixpair:C|R`、`C|U`、`C|C`、`C|L` 正值：电容类组合最倾向共置

## 5. 结论

**能做监督/模仿学习，已跑通并可复现。** 结论有三点：

1. 数据够用：1,489 块板、9.9 万条带网络和坐标的元件记录。
2. 任务成立：仅凭网表就能以 AUC≈0.87 预测人类是否把两个元件放在一起。
3. 模型已有增益：GNN 比线性基线 AUC 提升约 0.016，方向正确但幅度有限，
   说明当前特征/图结构还有明显提升空间。

## 6. 已知限制（如实说明）

- 标签是人类实际布局派生，不是电气最优解，含历史习惯与错误。
- GNN 用了 1,352 板 / 743,957 对，逻辑回归用了 1,446 板 / 717,600 对，
  两者子集不完全相同，属于同源但非严格同集的对比。
- 只到元件级中心距离；焊盘坐标已算出，但旋转方向约定未逐板验证。
- 未使用 2,195 个 `layout-process` 日志（含时间戳）与 1,466 个 `routing` JSON（真实走线）。

## 7. 下一步建议

1. 把"放置顺序"做成序列模仿（用 layout-process 时间戳），更接近逐步决策。
2. 用 routing JSON 的真实线长替换距离阈值标签，让目标更贴近电气质量。
3. GNN 换成更强的编码器（多头注意力 / 边特征网络），并加入网络类型与电流等级。
4. 把亲和度模型接入图划分/聚类，输出分区方案并与人类分区对比。

## 8. 产物

本地（`ml-dataset/`）：
- `data/*.csv` 统一数据集
- `model/metrics.json`、`model/feature_weights.json`、`model/训练报告.md`、`model/baseline_train.log`
- `gnn/gnn_metrics.json`、`gnn/gnn_top_pairs.csv`、`gnn/gnn_train.log`
- `build_unified_dataset.py`、`train_affinity_baseline.py`

服务器（`/root/pcb-ml/`）：
- `data/*.csv`、`train_gnn.py`、`gnn/`、`model/`、`gnn_train.log`、`baseline.log`

复现命令（服务器）：
```
cd /root/pcb-ml
/root/miniconda3/bin/python train_affinity_baseline.py
/root/miniconda3/bin/python train_gnn.py --folds 5 --epochs 12
```

---

# 补充：最终可部署模型（在云服务器上训练完成）

## 训练与验证口径

- 数据：1,352 块板 / 743,957 个候选对 / 981 个项目
- 按项目 3 路划分：训练 686 / 验证 147 / 测试 148 个项目（测试集完全未参与训练）
- 训练环境：V100-32GB，torch 2.0.1+cu118，12 轮，耗时 89.5 秒

## 测试集结果

| 指标 | 数值 |
|---|---:|
| 配对级 ROC-AUC | **0.8628** |
| 配对级 PR-AUC | 0.5728 |
| 聚类阈值 tau | 0.20 |
| 聚类 P / R | 0.599 / 0.488 |
| 聚类 F1 | 0.538 |

## numpy 推理一致性验证

导出的权重用纯 numpy 重新实现前向，与原始 torch 权重逐元素比对：

- 最大绝对误差 **3.9e-07**（数值等价）
- 同批 250 块板 / 341,682 对上的 AUC = 0.8607，与服务器测试集 0.8628 吻合

## 部署产物

`ml-dataset/deploy/`

- `一键划分.bat` —— 把网表文件或文件夹拖上去即可
- `partition_by_netlist.py` —— 网表解析 + numpy 推理 + 聚类 + 报告
- `validate_model.py` —— numpy/torch 一致性自检
- `model/partition_model.npz`、`model/vocab.json`、`model/final_metrics.json`
- `test_output/` —— 用"数字电源"项目 3 块板的真实网表跑出的样例结果

## 样例结果（数字电源 - 功率板，91 个元件）

模型自动分出的部分功能簇：

| 簇 | 元件 | 判断依据（共享网络） |
|---|---|---|
| 1 | U1、U11、L1、C13、C15、D3、R7~R9、R42~R44 | 主变换级 + 反馈网络 |
| 2 | Q1、U5、D5、D6、C25、R14、R16~R19 | 功率管 + 驱动 IC + 栅极电阻 |
| 3 | Q4、U2、D8、D9、C29、R11~R13、R20、R26 | 功率管 + 驱动 IC + 栅极电阻 |
| 4 | C1~C4 | 输入母线电容（GND / +24V） |
| 5 | D11、R22、R24、VCAP1 | V_CAP 吸收/泵电路 |
| 6 | Q2、D4、R15、R21 | A_LO 支路 |
| 7 | Q3、D7、R10、R27 | B_LO 支路 |
| 12/13 | LED2+R35、LED3+R36 | 指示灯 + 限流电阻 |
| 15 | R5、R6 | FB 反馈分压 |

这些簇与功率电源的功能分区高度吻合，说明模型学到的是真实的设计习惯，而不是噪声。

## 已知限制

- 聚类 F1 ≈ 0.54，说明**配对排序可用，但簇粒度需要人工复核**（去耦电容等只走电源网络的器件容易落单）。
- 阈值 `--tau` 和 `--max-size` 可按项目类型微调。
- 训练标签来自人类布局，含历史习惯与错误，不是电气最优解。
