# 功率PCB布局划分模型 — 训练方法总结

> 目标：从真实 PCB 设计中学习"哪些元件应该被划分到同一个功能簇"，
> 并对任意项目的**原理图网表**自动输出功能划分结果。
>
> 本文档总结训练全过程中使用的方法、数据、模型、评估与部署流程。

---

## 1. 数据构建

**来源**：`pcb-layout-projects` 目录下 1,489 个 EasyEDA 布局 JSON 文件，覆盖 1,010 个项目。

**解析内容**：

- 元件：位号（字典键）、取值、器件名、封装、层、旋转、坐标
- 连接关系：pad → net 映射
- 封装焊盘几何（用于还原焊盘绝对坐标）
- 原理图 `schematicInfo` 中的 `connectWith` 与 `group`
- 板框 outline / bbox

**产出的 6 张标准表**：

| 表 | 行数 | 说明 |
|---|---:|---|
| projects | 1,010 | 项目级汇总 + 分类/标题/简介 |
| boards | 1,489 | 一块板一行，含板框 |
| components | 99,109 | 元件摆放，100% 带坐标 |
| pad_nets | 379,691 | pad→net，全部算出焊盘绝对坐标 |
| nets | 80,860 | 网络度数 + 是否全局网络 |
| schematic_connections | 163,175 | 原理图 connectWith 连接 |

**两个关键处理**：

- **坐标补全**：用"元件坐标 + 旋转变换后的焊盘相对坐标"计算每个焊盘的绝对坐标。
- **全局网络判定**：用正则识别 GND / VCC / VDD / VIN / VOUT / 各电压轨（3V3、5V、12V、24V 等），
  用于区分"电源网络"与"信号网络"。

**防泄漏设计**：以**项目 UUID** 为分组键，同一项目的多块板、多版本图纸绝不跨越训练集与测试集。

---

## 2. 任务定义（模仿学习 → 配对二分类）

把"元件该怎么分区"这个连续、主观的问题，转化为可监督、可评估的判定问题：

- **候选对**：同一块板上、共享 ≥1 个网络的两个元件。
- **标签来自人类实际布局**（这就是"模仿"的对象）：
  - 同一层 且 中心距 ≤ 5 mm → **正样本**
  - 距离 ≥ 15 mm → **负样本**
  - 5–15 mm → **丢弃**（模糊区，不参与训练）
- 模型学习的是"人类设计者会不会把这两个元件放在一起"。

---

## 3. 特征工程

**核心约束：只使用网表与元件属性，绝不使用坐标、层、旋转**，从根本上杜绝空间泄漏。

共 57 维特征：

- **共享网络**：共享网络数量、信号/电源网络拆分、是否含信号网络、
  共享网络的最小/平均度数、低度数网络计数（degree ≤ 2、≤ 4）
- **元件属性**：各自网络数、引脚数、两者之和、差值的绝对值
- **属性相似度**：同位号前缀、同取值、同封装
- **图结构**：共同邻居数（2-hop 支撑度）
- **类别组合**：位号前缀组合 one-hot（取最常见的 40 组，如 C|R、C|U、Q|R）

---

## 4. 三个递进模型

| 模型 | 方法 | ROC-AUC | PR-AUC |
|---|---|---:|---:|
| 规则基线 | 共享信号网络计数线性打分 | 0.6961 | 0.2645 |
| 逻辑回归 | numpy 手写梯度下降（400 轮，L2 = 1e-3），特征标准化 | 0.8551 | 0.6120 |
| **混合 GNN** | PyTorch，二部图消息传递 + 成对 MLP | **0.8709** | 0.5968 |

### 4.1 GNN 结构

- **图构建**：元件节点 + 网络节点，边 = pad-net 连接，构成二部图。
- **节点编码**：
  - 元件侧：位号前缀 / 取值 / 封装 各 16 维 Embedding，拼接 log 网络数与引脚数 → `Linear(50 → 64)`
  - 网络侧：log 度数 + 是否全局网络 → `Linear(2 → 64)`
- **消息传递**：3 层**双向均值聚合**
  - 网络节点聚合其连接的元件，元件节点聚合其连接的网络；
  - 每层把"自身表示"与"聚合到的邻居表示"拼接后过线性层 + ReLU。
- **成对打分头**：
  `[h_a, h_b, h_a ⊙ h_b, |h_a − h_b|, 10 维配对特征]` → `MLP(266 → 64 → 32 → 1)` → Sigmoid
- **训练设置**：
  - 损失 `BCEWithLogitsLoss`
  - 优化器 Adam，学习率 2e-3
  - 12 轮
  - 小批量以"电路板"为单位，每 64 块板累积一次梯度后更新

### 4.2 训练环境

- 云服务器 Tesla V100-PCIE-32GB
- Python 3.8.10，torch 2.0.1 + cu118
- 全量训练耗时约 90 秒

---

## 5. 评估协议

- **按项目分组切分**：
  - 前期用 5 折交叉验证做模型选型；
  - 最终模型改为 70 / 15 / 15 三路划分（训练 686、验证 147、测试 148 个项目）。
- **指标**：ROC-AUC（并列值按平均秩处理）、PR-AUC（平均精度）。
- **对照基线与手写规则**，确认机器学习确实带来增益。
- **最终测试集结果（148 个完全未参与训练的项目）**：
  - 配对级 **ROC-AUC = 0.8628**
  - 配对级 **PR-AUC = 0.5728**

---

## 6. 从配对概率到功能簇

模型输出的是"两两之间该不该靠近"的概率，需要聚合成簇：

1. 初版用「阈值 + 并查集」聚合，出现问题了——传递性导致 36 个元件粘成一个巨型团。
2. 改为**贪心边合并**：
   - 把所有配对按概率从高到低排序；
   - 依次尝试合并，仅当 `概率 ≥ tau` **且** 合并后簇大小不超过 `max_size` 时才合并；
   - 这样既保证高置信度优先，又避免大簇粘连。
3. **阈值调优**：在验证集上以聚类 F1 为目标搜索 tau，得 **tau = 0.20**。
4. **聚类评估**：以"同层且 ≤5mm"的真实配对为正样本，计算成对精确率 / 召回率 / F1。
   - 测试集：**P = 0.599，R = 0.488，F1 = 0.538**

---

## 7. 部署与验证

- 模型权重导出为 `.npz`（Embedding 矩阵 + 各层权重偏置 + 阈值 tau）。
- 用**纯 numpy 重写前向传播**（`np.add.at` 实现索引聚合、`bincount` 实现度归一化），
  部署端**不需要 torch**，只需 numpy。
- **一致性校验**：numpy 实现与原始 torch 权重逐元素比对，
  **最大绝对误差 3.9×10⁻⁷**（数值等价）；同批数据 AUC 0.8607，与服务器测试集吻合。
- **网表解析**：支持 EasyEDA 的 `.enet` 网表；
  用位号正则过滤掉被误当作元件的电源端口（`+12V`、`GND`、`CAP` 等）。

---

## 8. 明确没有使用的方法

- **不是强化学习**。整套流程是监督学习 / 模仿学习。
  之前讨论过的 AlphaGo 式自对弈**没有落地**，原因是缺少可靠的物理模拟器与客观奖励函数。
- 没有使用图像或视觉模型。
- 焊盘绝对坐标虽然已算出，但没有进入模型（目前只到元件级）。
- 2,195 个 `layout-process` 日志（含时间戳、可还原放置顺序）
  与 1,466 个 `routing` JSON（含真实走线，可算实际线长）**尚未使用**。

---

## 9. 结论与限制

**结论**：仅凭网表拓扑与元件属性，就能以 **AUC ≈ 0.86** 预测人类是否会把两个元件放在一起；
对"数字电源"项目的实测输出（主变换级、功率管+驱动 IC、母线电容、吸收电路、反馈分压、指示灯等）
与真实功率电源的功能分区高度吻合。

**限制（必须如实看待）**：

- 聚类 F1 仅约 0.54 —— **配对排序可靠，但簇的粒度仍需人工复核**。
  只走电源网络的去耦电容容易落单，可通过 `--tau` 调低、`--max-size` 调大来合并。
- 训练标签来自人类布局，包含历史习惯与错误，不是电气最优解。
- 输出属于"常见做法 + 高置信度建议"，不是电气硬约束。
  功率回路、安规间距、散热、EMC 必须由工程师按数据手册与设计规则确认。

---

## 10. 产物位置

**本地**（`...\Partition\ml-dataset\`）：

- `data\` — 6 张统一数据集表
- `model\` — 逻辑回归基线的指标与特征权重
- `gnn\` — GNN 五折结果
- `deploy\` — **可部署包**
  - `一键划分.bat` — 把网表文件或文件夹拖上去即可运行
  - `partition_by_netlist.py` — 网表解析 + numpy 推理 + 聚类 + 报告
  - `validate_model.py` — numpy/torch 一致性自检
  - `model\partition_model.npz`、`model\vocab.json`、`model\final_metrics.json`
  - `test_output\` — 用"数字电源"项目真实网表跑出的样例结果
- `报告_模仿学习建模.md` — 完整建模报告

**云服务器**（`/root/pcb-ml/`）：

- `data\`、`train_gnn.py`、`train_final.py`、`train_affinity_baseline.py`
- `final\` — 最终模型权重与指标
- `final_train.log`、`gnn_train.log`、`baseline.log`

---

## 附：一句话概括方法链

> 用 1,489 块真实 PCB 版图的**元件坐标**生成"是否共置"的标签，
> 用**仅含网表信息**的 57 维特征训练**二部图 GNN** 学习元件间亲和度，
> 再通过**带簇大小上限的贪心边合并**把亲和度聚成功能簇，
> 最终导出为**纯 numpy 可运行**的一键划分工具。
