# 功率PCB硬约束目标函数形式化（EMI/EMC、热管理、间距/安规、功率回路）

> 面向框架：Cypress 类解析式布局。
> 目标：把功率PCB的“硬约束/工程规则”写成**可微、可加权的目标项**，并入
> `L = WL + λ_D·D + Σ λ_k·P_k`，供解析式算法做梯度优化。
> 配套文档：《功率PCB布局Partition设计方案.md》。

---

## 0. 总框架

解析式布局的标准形式：

```
L(x, y, θ) = λ_WL · WL(x, y, θ)
           + λ_D   · D(x, y, θ)          // 密度/重叠（Cypress 的静电密度）
           + Σ_k λ_k · P_k(x, y, θ)      // 功率PCB特化目标
```

`x, y, θ` 是器件中心坐标与朝向；`P_k` 是本文定义的特化项。对**硬约束**优先用
**augmented Lagrangian / 惩罚项**处理，保证“尽量满足”，并在 legalization 阶段做硬性修正。

**核心原则**：
1. 能用“几何距离/面积”代理的，先不用场仿真（保证快速、可微）。
2. 高频场问题（EMI 辐射）用**回路面积、dv/dt 节点暴露、敏感网络隔离**三个代理项近似。
3. 热问题用**功耗密度 + 热源分散 + 散热边界吸引**代理，真正的结温/铜温由后续热仿真签核。
4. 所有项统一写成“二次 + hinge/barrier + log-sum-exp”的可微形式，权重 `λ_k` 随迭代调度。

---

## 1. 约束分类与“硬/软”处理原则

| 类别 | 示例 | 建议处理方式 |
|---|---|---|
| 硬几何约束 | 板框、禁布区、器件重叠、允许朝向 | 解析式阶段用强惩罚，最终 legalization 强制满足 |
| 硬安全约束 | 爬电/电气间隙、初/次级隔离 | 强 hinge 惩罚 + keepout；最终按标准校验 |
| 功率性能目标 | 回路面积、栅极回路、去耦距离 | 软目标，权重中等 |
| EMI/EMC 目标 | 网络分离、dv/dt 暴露、敏感隔离 | 软目标 + 硬最小间距 |
| 热目标 | 热源分散、散热边界距离 | 软目标，权重中低 |

“硬”并不等于永远不可违反，解析式阶段是**软化的硬约束**（惩罚），最后用
**spacing-aware legalization**（参考 NTU Modern / Cypress 的 halo）恢复。

---

## 2. 逐类目标函数

下面用 `p` 表示 pad 坐标（由器件中心 + 朝向算得），`x` 表示器件中心。
`[z]_+ = max(0, z)`，`H(z)` 为光滑 hinge（如 softplus `log(1+exp(z))`）。

### 2.1 功率回路 / 高 di/dt 回路面积（最重要）

**工程依据**（TI SLVA773 / SLVAFJ3 / SLYT614，Infineon 栅极驱动指南）：
输入电容、开关管、输出电容、地之间的**脉冲电流回路**面积要最小，否则 `V = L·di/dt`
产生电压尖峰、振铃与 EMI。

**回路表示**：一个 hot loop 是若干 pad 的有序集合 `Loop = {p_1, p_2, …, p_m}`。
其面积可用 **shoelace（鞋带公式）** 计算，对 pad 坐标可微：

```
A(Loop) = 1/2 · | Σ_{t=1..m} (p_t.x · p_{t+1}.y − p_{t+1}.x · p_t.y) |
```

**目标项（面积）**：
```
P_loop = Σ_{L ∈ Loops} w_L · A(L)²
```
`w_L` 按回路电流的 `di/dt`（或 `I_peak·f_sw`）加权。

**代理项（更稳、更易算）**：当 pad 顺序未知时，用回路内所有 pad 的
**成对平方距离**近似“紧凑度”，避免 shoelace 在共线/凹形时的梯度不稳定：

```
P_loop_proxy = Σ_{L} w_L · ( 1/|L| · Σ_{i,j∈L, i<j} ‖p_i − p_j‖² )
```

两种形式都直接接入梯度。解析式全局布局建议先用 `P_loop_proxy`，局部精调再用 `A(L)`。

**注意**：回路用 **pad 坐标** 而非器件中心，才能捕获“输入电容必须贴 IC 引脚”这类约束。

---

### 2.2 高 dv/dt 节点（SW node）暴露最小化

**工程依据**：SW 节点是开关电源最主要的 EMI 源，其**铜皮面积要最小**，且不能长距离走线。

**目标项**：把同一 SW 网络的 pad 拉紧（缩小 SW 铜区），同时限制 SW 区域尺寸。

```
P_sw = λ_sw · Σ_{n ∈ SW_nodes} ‖p_n − c_SW‖²
```
`c_SW` 为 SW 节点 pad 的质心；此项把所有 SW pad 压向质心，等价于“SW 铜皮最小”。

**面向可布线性**：还可把 SW 铜区面积写成 pad 凸包面积 `A(hull(SW))`，作为上界惩罚：

```
P_sw_area = λ_sw · A( hull(SW_pads) )²
```

---

### 2.3 敏感节点隔离（FB / VOS / CS / 栅极输入）

**工程依据**：反馈、输出电压采样、电流采样等敏感走线要**远离 SW 节点、磁性件与栅极走线**，
否则噪声耦合导致稳压差、抖频甚至失效。

**目标项**：敏感节点 pad 与噪声节点 pad 之间的最小距离，用 **hinge 排斥**：

```
P_sense = Σ_{s∈S} Σ_{n∈N} w_{s,n} · [ d_min(s,n) − ‖p_s − p_n‖ ]₊²
```
`S` = 敏感节点集（FB、VOS、CS、AGND、PWM 输入），`N` = 噪声节点集（SW、gate、磁性件）。
`d_min` 是建议最小间距（可由规则库给定，如“FB 距 SW ≥ 5 mm”）。当距离足够时惩罚为 0。

**进阶**：用 log-sum-exp 平滑多个最近邻，避免 hinge 的稀疏梯度：

```
P_sense_smooth = Σ_{s∈S} (1/β) · log Σ_{n∈N} exp( β·(d_min − ‖p_s − p_n‖) )
```

---

### 2.4 栅极驱动回路

**工程依据**（Infineon）：driver 要尽量贴近 MOSFET，缩短 gate/source 高电流走线；VDD 与 GND 走线成对贴近。

**目标项**：
```
P_gate = λ_gate · Σ_{(drv,mos)} [ ‖x_drv − x_mos‖² + Σ_{g,s} ‖p_gate − p_source‖² ]
       + λ_byp  · Σ_{byp} ‖p_byp − p_drv_vdd/gnd‖²
```
- 第一项：driver 与 MOSFET 中心距离 + gate/source pad 距离（缩小栅极回路）。
- 第二项：VDD 旁路电容贴 driver 引脚。

---

### 2.5 去耦 / 旁路电容贴装

**工程依据**：输入电容贴 IC（PVIN/PGND）、输出电容贴 IC 或二极管、自举电容贴 SW/BOOT。

**目标项**（与 partition 的“包”结合，直接在包内 pad 级施加）：
```
P_dec = λ_dec · Σ_{(cap,pin_pair)} ‖p_cap_1 − p_ic_1‖² + ‖p_cap_2 − p_ic_2‖²
```
`pin_pair` 表示电容两个引脚分别对应的 IC/功率器件引脚（电源 + 地）。

---

### 2.6 网络分离 / 串扰 / 可布线性

**工程依据**：不同功能网络（尤其功率网络与敏感信号网络）应尽量不交叉、不并行走线，降低串扰与布线拥塞。

**目标项（NS-Place 风格，最大间隔）**：
对每对“需要分离”的网络 `(net_a, net_b)`，取两个网络的 pad 凸包 `H_a, H_b`，要求其间隔最大：

```
P_sep = Σ_{(a,b)} [ m_ab − gap(H_a, H_b) ]₊²
```
`gap(H_a,H_b)` 为两个凸包的最小距离；`m_ab` 为期望最小间隔。

**目标项（Cypress 风格，net crossing）**：对关键敏感网络对，惩罚其 Bézier/导线段的相交程度。
在全局布局阶段，可退化为“pad 级凸包距离”代理，见 §2.3。

---

### 2.7 间距 / 爬电距离 / 电气间隙（安规）

**工程依据**：不同电压等级、不同隔离域之间要满足 **clearance（空气间隙）与 creepage（爬电距离）**，
由工作电压、污染等级、CTI/材料组、海拔等决定（IEC 60664-1 / IPC-2221）。不能写死为单一数值。

**目标项**：对每个需要间距约束的器件/网络对，施加 hinge 惩罚：

```
P_clear = Σ_{(i,j)} w_{ij} · [ c_ij − ‖x_i − x_j‖ ]₊²
```
`c_ij` 来自规则库：`c_ij = clearance_rule(voltage_i, voltage_j, pollution_degree, material_group, altitude)`。
**必须保留 `source/version/condition/priority` 元数据**，最终仍按标准做 DRC 校验。

**禁布区（keepout）**：变压器、散热器、隔离槽等区域写成“到 keepout 区域的最小距离”：

```
P_keepout = Σ_i [ r_i + margin − dist(x_i, keepout) ]₊²
```

---

### 2.8 热管理

**工程依据**（SLYP762 热管理章、SLVA773 铜皮改善散热）：高功耗器件要**分散**，靠近散热边界/大面积铜/热过孔，避免热源聚集形成热点。

**热源密度项（仿静电密度）**：把每个器件视为热源 `q_i = P_i`（功耗 W），定义热势场
`∇²φ = −ρ_th(x)`，其中 `ρ_th(x) = Σ_i q_i · K(x − x_i)`（K 为高斯/平滑核）。目标为
**最小化热势能 / 热点峰值**：

```
P_thermal = ∫ ρ_th(x) · φ(x) dx   ≈   Σ_i q_i · φ(x_i)
```
`φ` 由 Poisson 方程 + FFT 求解（与 ePlace/RePlAce/DREAMPlace 密度算子完全同构，可直接复用其代码）。
它的效果：把热源均匀摊开，避免高功耗器件叠在一起。

**散热边界吸引项**：鼓励高功耗器件靠近散热边界/热过孔区：

```
P_cool = − Σ_i q_i · dist(x_i, thermal_keepin)²
```
`thermal_keepin` 为散热铜区/散热器/板边热释放区（吸引，而非排斥）。

**热源分散代理（无 FFT 时的简化）**：
```
P_thermal_proxy = Σ_{i,j∈H} q_i·q_j · max(0, R_min − ‖x_i − x_j‖)²
```
`H` 为高功耗器件集合，`R_min` 为最小热间距。此项惩罚高功耗器件“扎堆”。

---

### 2.9 接地（PGND / AGND 单点汇合）

**工程依据**（SLVAFJ3）：功率地与模拟地分离，仅在 thermal pad / 单点连接，避免功率大电流干扰模拟地。

**目标项**：把 PGND 簇质心与 AGND 簇质心引向同一“单点星形点” `s_*`：

```
P_gnd = Σ_{g∈G} w_g · ‖C_g − s_*‖²
```
`C_g` 为各地簇的 pad 质心；`s_*` 通常取控制器 thermal pad。此项强制单点接地拓扑。

---

### 2.10 EMI 端口/天线（可选）

**工程依据**：开关噪声回路是一根“天线”，其辐射与**回路面积 × 电流 × 频率²**相关；输入端口/连接器附近应远离 SW 与大回路。

**目标项**：
```
P_emi = Σ_{L} (I_L · f_sw,L)² · A(L)²
       + Σ_{port, L} [ d_port − dist(port, hull(L)) ]₊²
```
第一项按辐射强弱对回路面积加权；第二项让大回路远离输入/输出端口。

---

## 3. 平滑化与可微性处理

| 原始量 | 问题 | 替代 |
|---|---|---|
| HPWL | 不可微 | 二次型 / WA / LSE |
| `[z]_+`（hinge） | 二阶导不连续 | `softplus(z)` 或 `z²`（平方 hinge） |
| `max(0, d−‖·‖)` 的梯度稀疏 | 多数时候为 0 | log-sum-exp 汇聚最近邻 |
| 凸包面积 `A(hull)` | 求 hull 的分段梯度 | pad 成对平方距离代理，或仅局部精调阶段使用 |
| 静电密度 `∇²φ=ρ` | 需 FFT | 复用 ePlace/DREAMPlace 的 density 算子 |
| 热势场 `∇²φ=ρ_th` | 需 FFT | 同静电密度，或 `P_thermal_proxy` |

**通用形式**：所有功率特化项都尽量写成“二次弹簧（吸引）+ 平方 hinge（排斥）+ 密度势能”的组合，
这样可直接进入现有解析式求解器的梯度/预条件框架。

---

## 4. 权重调度策略（λ 怎么随时间变）

参考 Cypress/DREAMPlace 的密度权重调度，功率项也**不要一开始就满权重**：

| 阶段 | λ_WL | λ_D | λ_loop/λ_gate/λ_dec | λ_iso/λ_clear | λ_thermal |
|---|---|---|---|---|---|
| 早期（粗布局） | 高 | 中 | 中 | 中 | 低 |
| 中期 | 中 | 指数上升 | 中高 | 高 | 中 |
| 后期（精调） | 中低 | 高 | 高 | 最高（硬） | 中高 |
| 合法化前 | 低 | — | — | 转成硬约束 | — |

**规则**：
1. 密度权重 `λ_D` 按迭代**指数增长**（经典做法）；
2. 硬约束（间距/隔离）在后期**线性增长**到高值；
3. 软目标（回路、栅极、去耦、热）保持中等，避免压过线长/密度导致无法收敛；
4. 最后用 spacing-aware legalization 把“软化的硬约束”真正满足。

---

## 5. 与 Partition 的关系

Partition 生成“包”后，本文目标项直接作用于**包内 pad 坐标**：

| Partition 输出 | 对应的目标项 |
|---|---|
| 功率级包（hot loop） | `P_loop` / `P_sw` |
| 栅极驱动包 | `P_gate` |
| 去耦/旁路元件归属 | `P_dec` |
| 电压域组 / 隔离域 | `P_clear` / `P_keepout` / `P_sep` |
| 热分区 / 散热区 | `P_thermal` / `P_cool` |
| 地簇 | `P_gnd` |

即：**先 partition 定“谁和谁是一个回路/一个包”，再用本文的 `P_k` 定义“这个包应该多紧凑、离谁多远”。**

---

## 6. 实现检查清单

- [ ] 定义 pad 级坐标接口：`(x, y, θ) → pin(x,y)`，朝向用 Gumbel-Softmax 或离散枚举（参考 Cypress）。
- [ ] 实现 `softplus / square-hinge / log-sum-exp` 三种基本可微算子。
- [ ] 实现静电密度算子（Poisson+FFT），并让热密度复用同一算子。
- [ ] 从网表解析 hot loop / SW node / sense node / gate loop / 去耦对 / 隔离域。
- [ ] 把每个 `P_k` 写成独立模块，输出 `value + gradient`，便于单元测试与权重调参。
- [ ] 建立规则库：`clearance_rule`、`thermal_keepin`、`keepout`、`d_min`，全部带 `source/version/priority`。
- [ ] 用一个小功率板（如 LM5177 或 Infineon 栅极驱动局部电路）跑通，画收敛曲线与热力图。

---

## 7. 公式速查表

| 工程规则 | 数学目标项 | 可微形式 | 权重 |
|---|---|---|---|
| 脉冲电流回路小 | `Σ w_L·A(L)²` 或 pad 成对 `‖p_i−p_j‖²` | 面积/二次 | λ_loop |
| SW 铜皮小 | `Σ‖p_n−c_SW‖²` | 二次 | λ_sw |
| 敏感远离噪声 | `Σ[d_min−‖p_s−p_n‖]₊²` | 平方 hinge | λ_sense |
| driver 贴 MOSFET | `‖x_drv−x_mos‖² + Σ‖p_g−p_s‖²` | 二次 | λ_gate |
| 去耦贴 IC | `Σ‖p_cap−p_pin‖²` | 二次 | λ_dec |
| 网络分离 | `Σ[m−gap(H_a,H_b)]₊²` | 平方 hinge | λ_sep |
| 爬电/间隙 | `Σ[c_ij−‖x_i−x_j‖]₊²` | 平方 hinge | λ_clear |
| 禁布区 | `Σ[r_i+margin−dist]₊²` | 平方 hinge | λ_keepout |
| 热分散 | `∫ρ_th·φ dx` 或高功耗对 `[R_min−‖·‖]₊²` | 密度势能/平方 hinge | λ_thermal |
| 单点接地 | `Σ‖C_g−s_*‖²` | 二次 | λ_gnd |
| EMI 辐射 | `Σ(I·f)²·A(L)²` | 面积 | λ_emi |
