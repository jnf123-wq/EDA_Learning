# 解析式（Analytical）布局算法：方法与模型汇总

**方向：功率 PCB 自动布局算法设计与优化**  
**整理时间：2026 年 8 月**

---

## 0. 通用框架（先记住这张"骨架"）

解析式布局 = 把布局写成**可微目标函数**，用数值优化求解。标准三阶段：

```
全局布局（global placement，可微目标 + 梯度下降）
   → 合法化（legalization，消除重叠/违规，得到严格合法解）
   → 详细布局（detailed placement，局部精修）
```

解析式布局的"方法/模型"可拆成四块：**线长模型、密度模型、求解器、合法化**。具体 placer（ePlace、DREAMPlace、Cypress…）都是这四块的组合。

---

## 1. 线长模型（Wirelength Models）

| 模型 | 形式 / 思想 | 特点 |
|---|---|---|
| **HPWL（半周长线长）** | $\mathrm{HPWL}(e)=\max_{a\in e}x_a-\min_{a\in e}x_a+\max_{a\in e}y_a-\min_{a\in e}y_a$ | 布线长度最常用估计，但 `max/min` **不可微** |
| **二次线长（Quadratic）** | $W_q(e)=\sum_{(i,j)\in e}\big[(x_i-x_j)^2+(y_i-y_j)^2\big]$ | 光滑、凸、可微，但**过度惩罚长线**、易过度扩散；Gordian/Kraftwerk 用 |
| **平滑 HPWL（log-sum-exp，LSE）** | $W_\gamma(e)=\gamma\Big[\log\sum e^{x_a/\gamma}+\log\sum e^{-x_a/\gamma}+\log\sum e^{y_a/\gamma}+\log\sum e^{-y_a/\gamma}\Big]$ | 用 **γ 退火（continuation）**逐步逼近真实 HPWL；当前主流平滑方式 |
| **加权平均模型（WA，Weighted-average）** | 用 softmax 加权平均近似 max/min | LSE 的等价/近似写法，同样用于可微化 |

> 关键思想：**"先平滑、后收紧"**——大 γ 时目标平滑易搜索，后期 γ→0 逼近真实 HPWL，比从一开始用尖锐目标更稳定。

---

## 2. 密度 / 重叠模型（Density & Overlap Models）

| 模型 | 代表工作 | 思想 |
|---|---|---|
| **二次密度惩罚** | 经典 / Gordian | 密度溢出平方惩罚，简单但鲁棒性一般 |
| **静电密度（Electrostatics）** | **ePlace** | 把器件面积视为**电荷**，密度 overflow 转电势能；用 Poisson 方程 $\nabla^2\phi=\rho$ + **FFT** 求电势与电场，器件沿负梯度从高密度区扩散 |
| **局部密度函数** | **RePlAce** | 用**局部（local）密度函数**替代全局静电，配合**动态步长**，兼顾线长与可布性 |
| **多静电系统 + 密度乘子** | **elfPlace / eLfPlace** | 每类资源（宏/单元）独立静电系统 + 密度乘子向量 $\lambda$，支持**混合尺寸（mixed-size）** |
| **多静电 + 区域约束 + 熵注入** | **DREAMPlace 3.0** | 多静电系统 + 区域约束 + **自适应二次惩罚/熵注入**，收敛更稳 |
| **PCB 双面/旋转密度图** | **Cypress** | Top/Bottom **分别建 density map**，旋转后按新宽高重新计算占用 |

---

## 3. 求解器 / 优化方法（Solvers）

| 求解器 | 说明 / 使用方 |
|---|---|
| **Nesterov 加速梯度** | ePlace / RePlAce 首选，收敛快 |
| **Adam** | 一阶自适应，常用替代 |
| **L-BFGS / 预条件梯度** | 近似二阶，稳定 |
| **GPU / PyTorch 化** | **DREAMPlace**：把目标与梯度算子写成神经网络前向/反向，GPU 加速 |
| **可微联合优化** | **Cypress**：对**位置 + 旋转角**做可微优化（方向用 softmax/Gumbel 软松弛后 snap） |

---

## 4. 合法化方法（Legalization）

| 方法 | 说明 |
|---|---|
| **QP 合法化** | 二次规划，在保持相对位置下消除重叠 |
| **约束图（Constraint graph）** | 一维间距约束图 + 拓扑排序求解 |
| **窗口合法化（Window legalization）** | 分窗口局部合法化后拼接 |
| **Macro halo** | 源自 **AutoDMP**，处理**宏模块/大器件的间距约束**；**Cypress 用其处理 PCB 间距/固定器件** |
| **Hanan 网格合法化** | Zhao/Hu 2026 的 PCB 三段式里采用 |
| **局部 SA / 贪心精修** | 详细布局阶段跳出局部最优 |

---

## 5. 具体 placer 实例（按流派/时间）

| Placer | 来源 | 核心模型 + 求解器 | 特点 |
|---|---|---|---|
| **Gordian** | 经典 | 二次规划 + 递归划分 | 二次线长 + 划分 |
| **Kraftwerk** | 经典 | 二次 / 力导向 | 二次全局布局 |
| **ePlace** | Cheng et al., TODAES 2014 | 静电密度 + FFT + Nesterov | 开创静电解析式 |
| **RePlAce** | UCSD | 局部密度 + 动态步长 + Nesterov | 优化线长与可布性 |
| **elfPlace / eLfPlace** | TCAD 2021 | 多静电 + 密度乘子 | 混合尺寸 |
| **DREAMPlace** | ICCAD 2019 | ePlace/RePlAce 的 **GPU/PyTorch** 版 | 大幅提速 |
| **DREAMPlace 3.0** | ICCAD 2020 | 多静电 + 区域约束 + 熵注入 | 鲁棒性增强 |
| **Xplace** | 学术 | DREAMPlace 扩展 + 神经网络 | 更快、可扩展 |
| **Cypress** | **ISPD 2025 最佳论文** | 朝向感知线长 + 密度 + net crossing；位置+旋转联合可微；macro halo 合法化 | **PCB 专用**，GPU，处理旋转/双面/间距/固定器件 |
| **Modern Automatic PCB Placement（DAC 2024）** | NTU 姚文川组 | 最小外接矩形 + 相似器件聚类 + **沿功率流向布锚点** | PCB 复杂间距约束 |
| **Analytical + fine-tuning（Zhao/Hu 2026）** | Integration/VLSI J. | 全局布局 → **Hanan 网格合法化** → fine-tuning | PCB 三段式，提升可布性 |
| **MARS-Place** | 武汉大学, Integration 2026 | 多阶段对齐精化 | PCB 布局+布线联合优化 |
| **Clearance-Constrained PCB Global Placement** | DAC 2025 | clearance 纳入解析式 | PCB 间距约束 |

---

## 6. PCB 特化扩展（与你课题直接相关）

| 扩展点 | 做法 |
|---|---|
| **旋转（离散方向）** | 连续松弛（softmax/Gumbel-softmax 软方向）+ 降温 snap；或交替优化；或多启动 |
| **双面（Top/Bottom）** | 分面建 density map；固定/禁布面作不可用容量 |
| **net crossing（可布性）** | 加入网络交叉数作为可布性代理（Cypress） |
| **clearance / 间距** | $\operatorname{softplus}(c_{ij}-d_{ij})^2$ 软惩罚 + 精确 legalizer 保证零违规 |
| **Power 专用目标** | $J_{loop}$（回路面积）、$J_{port}$（端口邻近）、$J_{noise}$（敏感/开关隔离）、$J_{thermal}$（功率加权热扩散） |

---

## 7. 模型 ↔ 方法 ↔ 实例 对应速查

```
线长模型：HPWL → 二次 → LSE/WA（平滑）
密度模型：二次惩罚 → 静电(ePlace) → 局部密度(RePlAce) → 多静电(elfPlace/DREAMPlace3)
求解器：Nesterov / Adam / L-BFGS → GPU(PyTorch, DREAMPlace) → 可微联合(Cypress)
合法化：QP / 约束图 / 窗口 / macro halo / Hanan 网格 / SA
实例：Gordian→ePlace→RePlAce→elfPlace→DREAMPlace→Xplace →【PCB】Cypress/DAC2024/Zhao2026/MARS-Place
```

> 一句话总结：解析式布局的**核心模型是"平滑线长 + 密度势"**，**核心求解手段是梯度下降（GPU 化）**，**PCB 化的增量是"旋转 + 双面 + net crossing + clearance + 功率目标"**。Cypress 是当前把这些 PCB 特性收得最全的开源解析式 placer，也是你自研功率 PCB placer 最合适的基座。