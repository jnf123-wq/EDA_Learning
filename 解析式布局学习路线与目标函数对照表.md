# 解析式布局学习路线与目标函数对照表

> 面向：功率PCB自动布局（Cypress 类解析式布局框架）。
> 目标：先把解析式布局的"模型 / 目标函数 / 求解器"这条主线吃透，再转向功率PCB特化约束的建模。
> 本文档对应 `EDA入门\解析式算法` 与 `EDA入门\其他布局算法` 两个文件夹中的论文。

---

## 一、精读顺序（建议按此推进）

### 第 0 步：先建立四个基础概念
1. **布局问题的标准形式**：`min WL(x,y)  s.t. 密度约束 / 无重叠约束`。
2. **线长模型**：HPWL（不可微）→ 二次型（quadratic）→ 平滑可微近似（weighted-average / log-sum-exp）。
3. **网表模型（net model）**：clique、star、hybrid、Bound2Bound（B2B）——决定连通矩阵的稀疏性与精度。
4. **密度/展开模型**：bin 惩罚、bell-shaped 势函数、静电密度（ePlace/RePlAce/DREAMPlace 的 Poisson+FFT）。

### 第 1 阶段：二次解析式（最简单，先复现这一类）
| 顺序 | 论文 | 一句话定位 |
|---|---|---|
| 1 | **FastPlace**（TCAD 2005） | 二次目标 + cell shifting + hybrid net model，最简洁的"复现起点" |
| 2 | **SimPL**（CACM 2013） | 二次 + 力导向 + B2B 网表模型 + look-ahead legalization |
| 3 | **GORDIAN-L**（DAC 1991） | 线性 vs 二次目标函数的源头讨论，建立目标函数选择直觉 |

### 第 2 阶段：非线性解析式（静电密度，现代主流）
| 顺序 | 论文/工具 | 一句话定位 |
|---|---|---|
| 4 | **APlace**（ICCAD 2004） | bell-shaped 密度势能 + 共轭梯度 + 拥塞估计 |
| 5 | **ePlace / RePlAce** | 静电密度模型（Poisson + FFT）的代表 |
| 6 | **DREAMPlace** | GPU + 深度学习优化器，现代解析式布局框架 |

### 第 3 阶段：PCB 特化（把硬约束写成目标函数）
| 顺序 | 论文 | 一句话定位 |
|---|---|---|
| 7 | **NS-Place**（ASP-DAC 2022） | 用 SVM 最大间隔写"网络分离"目标，PCB 可布线性 |
| 8 | **武大 analytical PCB placement**（Integration 2026） | WA 线长 + 静电密度 + phased rotation，Cypress 的"前作" |
| 9 | **Modern Automatic PCB Placement**（NTU, LBR） | 力导向 + 功率电路流向 + SA 对齐，和功率/间距最对口 |
| 10 | **MARS-Place**（Integration 2026） | SA + 对齐力/排斥力精调，PCB 信号完整性视角 |
| 11 | **Cypress**（ISPD 2025） | **主框架**：LSE 线长 + 静电密度 + net crossing + Gumbel-Softmax 朝向 + GPU |

---

## 二、目标函数公式对照表

| 论文/工具 | 线长项 WL | 密度 / 重叠项 | 额外项 | 求解方法 | 关键特点 |
|---|---|---|---|---|---|
| **FastPlace** | 二次型 `Σ w_ij·(xi−xj)²`（clique/star/hybrid 网表模型） | **cell shifting**（分 bin 把单元推向稀疏区，非光滑惩罚） | iterative local refinement（HPWL 局部优化） | 二次规划 / CG | flat、迭代式、易复现 |
| **SimPL** | 二次型 `Φ = Σ w_ij·(xi−xj)²`，用 **B2B 网表模型**逼近 HPWL | **look-ahead legalization**（几何划分 + 非线性缩放）作为伪网/锚点展开 | force modulation（上下界 gap 控制展开力） | Jacobi 预条件 + CG | 紧凑、自洽、上下界收敛机制 |
| **GORDIAN-L** | **线性**（曼哈顿）或 **二次**目标 | 迭代划分（partitioning）施加区域/质心约束 | 无 | QP + 划分 | 线性 vs 二次目标比较的经典 |
| **APlace** | 光滑线长近似 × WLWeight | **bell-shaped 势函数** + 平方偏差密度惩罚 | Kahng-Xu bend-based 拥塞估计 | 共轭梯度 CG | 密度光滑化的代表性做法 |
| **ePlace / RePlAce** | **weighted-average（WA）/ log-sum-exp** 线长 | **静电密度势能**，Poisson 方程 + FFT 求电场 | — | Nesterov / 梯度下降 | 现代静电密度模型的源头 |
| **DREAMPlace** | **log-sum-exp（LSE）** 线长 | 静电密度势能（同 ePlace/RePlAce） | 可选时序 / 拥塞 / fence 约束 | Adam / Nesterov，GPU | ML 优化器 + GPU 加速 |
| **NS-Place** | **weighted-average（WA）** 线长 | bin 平滑密度项 `D(x,y)` | **网络分离项 Ns**（SVM 最大间隔，分离不同 net 的凸包） | 坐标下降（交替优化 u,γ 与 x,y），MILP 合法化 | 把可布线性写成可微目标 |
| **武大 analytical** | **weighted-average（WA）** 线长 | 静电密度（ePlace/RePlAce 模型） | phased-rotation 朝向处理 | 梯度下降 | 比 Cypress 更简单，适合过渡 |
| **NTU Modern** | 力导向吸引力 `F = Σ w_ij·dx`（类弹簧） | 排斥力处理重叠 / clearance | 功率电路流向锚点、SA pad 对齐 | 力平衡 + SA + window legalization | 功率/间距约束最直接 |
| **MARS-Place** | **HPWL**（SA 代价） | **重叠面积 OA**（二次 + 线性惩罚） | 对齐弹簧力 + 排斥力（routing sequence 引导） | SA + 力平衡精调 | 混合式，PCB 信号完整性 |
| **Cypress** | **orientation-aware LSE 线长** `WL(x,y,θ)` | 静电密度 `D(x,y,θ)`（双面分别建密度图） | **net crossing `NC`**（Bézier 相交 + bell-shaped 平滑） | 梯度下降（Adam/Nesterov）+ 预条件，GPU | 你课题的主框架，三项目标 + Gumbel-Softmax 朝向 |

### 通用模板
解析式 PCB/VLSI 布局的"标准写法"基本统一为：

```
L(x, y, θ) = WL(x, y, θ) + λ_D · D(x, y, θ) + λ_extra · Extra(x, y, θ)
```

- `WL`：可微线长（二次型 / WA / LSE）。
- `D`：密度或重叠项（bin 惩罚 / bell-shaped / 静电密度）。
- `Extra`：你后续要加的**功率PCB特化项**（EMI/EMC、热、功率环、间距、朝向等）。
- `λ` 通常按调度逐步增大（密度权重指数增长，交叉/约束权重线性增长）。

---

## 三、复现建议（最小可行复现）

**目标**：先写一个"2D 二次解析式布局器"，跑通 `netlist → 求解 → 展开 → 可视化`，再逐步换成静电密度与 PCB 特化项。

1. **输入**：器件中心坐标、pin 偏移、netlist（可用 `.aux`/`.kicad_pcb` 或自己造的 JSON/CSV）。
2. **线长**：先实现二次型 `Σ w_ij·(xi−xj)²`，再升级到 WA / LSE。
3. **密度**：先做最简单的 cell shifting（FastPlace 思路），再换成静电密度（Poisson + FFT）。
4. **求解**：小规模直接 `numpy.linalg.solve` / 共轭梯度；上规模再考虑 GPU / PyTorch。
5. **合法化**：先 Tetris / Abacus 即可。
6. **可视化**：把每次迭代的器件位置、密度热力图、线长曲线画出来，方便 debug 和理解收敛。

---

## 四、开源实现清单（已核对）

| 项目 | 地址 | 说明 |
|---|---|---|
| **Cypress**（主框架） | https://github.com/NVlabs/Cypress | ISPD 2025 最佳论文，GPU PCB 布局，基于 DREAMPlace + AutoDMP |
| **DREAMPlace** | https://github.com/limbo018/DREAMPlace | GPU 解析式布局框架（LSE 线长 + 静电密度），Cypress 的底座 |
| **RePlAce** | https://github.com/The-OpenROAD-Project/RePlAce | 静电密度全局布局（CPU），OpenROAD 的 `global_placement` 来源 |
| **OpenROAD** | https://github.com/The-OpenROAD-Project/OpenROAD | 集成 RePlAce 的完整物理设计流程 |
| **AutoDMP** | https://github.com/NVlabs/AutoDMP | DREAMPlace 宏布局 + 多目标贝叶斯调参，Cypress 调参借鉴 |
| **nextpnr（HeAPPlacer）** | https://github.com/YosysHQ/nextpnr | FPGA 布局，`common/placer_heap.*` 实现了 SimPL 风格的二次+合法化思路，C++ 可读性好 |
| **FastPlace** | 原始代码难找（Iowa State 页面已基本失效） | 建议：读论文自己复现，或参考 DREAMPlace/RePlAce |
| **ePlace** | 未找到独立公开仓库 | 同一静电模型可用 RePlAce / DREAMPlace 代替 |
| **SimPL** | 未找到官方 GitHub 仓库 | 论文 + nextpnr 的 SimPL 启发式实现可作参考 |

### 复现路径建议
- **想跑 Cypress**：先按 DREAMPlace 的 README 配好环境（Linux + CUDA 更顺），再 clone `NVlabs/Cypress`，用 Docker 最省事。
- **想先理解二次布局**：读 FastPlace/SimPL 论文，照着写一个几十行 Python 的二次求解 + cell shifting 原型；同时看 nextpnr 的 `placer_heap.cc` 学习工程实现。
- **想复现静电密度**：直接跑 RePlAce（CPU）或 DREAMPlace（GPU），重点读 density 算子和 Poisson/FFT 部分。

---

## 五、下一步（结合你的课题）

在跑通"线长 + 密度"主链后，把学习重点转到：

1. **partition 设计**：参考 Cypress 的固定器件 / halo / 器件分组思路，以及 FastPlace 的 bin 网格和 clustering 思想，设计"功率器件 + 紧密器件打包、再按电压域打包"的层级结构。
2. **功率PCB特化目标函数**：
   - 功率回路 / 功率流 → 可仿 NTU 的锚点 + 吸引力；
   - EMI/EMC / 网络分离 → 可仿 NS-Place 的 margin / Cypress 的 net crossing；
   - 热管理 → 可仿静电密度思路，把"热源密度"当势能场写进目标；
   - 间距 / clearance → 可仿 Cypress 的 macro halo。
